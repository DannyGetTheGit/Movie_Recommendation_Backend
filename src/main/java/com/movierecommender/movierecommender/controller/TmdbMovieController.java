package com.movierecommender.movierecommender.controller;

// Retired. This logic has been merged into MovieController at /api/movies.
// The file is kept to avoid breaking git history — it has no Spring annotations
// and will not be loaded by the application.
class TmdbMovieController {
}
